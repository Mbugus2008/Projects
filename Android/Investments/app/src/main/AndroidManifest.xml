<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trimline.investments">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Investments"
        android:allowBackup="true"
        android:icon="@drawable/investment_logo_small"
        android:label="@string/app_name"
        android:roundIcon="@drawable/investment_logo_small"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".transfer"></activity>
        <activity
            android:name=".activefunds"
            android:label="@string/title_activity_activefunds"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Forgot_Password"
            android:label="@string/title_activity_forgot__password"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".addeditfund" />
        <activity android:name=".Realestatefund" />
        <activity android:name=".My_Properties" />
        <activity android:name=".My_booking" />
        <activity
            android:name=".mini"
            android:label="@string/title_activity_mini"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".balances"
            android:label="@string/title_activity_balances"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".float_shares" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Shares_Trading" />
        <activity android:name=".book" />
        <activity android:name=".propertydetails" />
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/actions" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>